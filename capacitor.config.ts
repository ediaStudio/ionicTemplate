import type {CapacitorConfig} from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'dha.app.template', // TODO remplacer par votre nom de package
    appName: 'dhaTemplate', // TODO remplacer par le nom de votre application sans espace
    webDir: 'www'
};

export default config;
